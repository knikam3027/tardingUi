version: "3.8"

services:
  trading-ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trading-ui
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
